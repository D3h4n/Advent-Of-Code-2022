// Package parser is responsible for parsing the various sections of the input file.
// It defines a crate parser, which parses the different stacks of crates and a command
// parser, which parses the list of commands that follows.
package parser
